using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HQLink : MonoBehaviour
{
    // Start is called before the first frame update
    public HQArea.Area area;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
